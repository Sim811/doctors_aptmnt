<h1>Add Doctor</h1>
<%= form_with(model: @doctor, local: true, class: "ui form") do |f| %>
  <div class = "field">
    <%= f.label :name %>
    <%= f.text_field :name, require: true, placeholder: "Doctor Name" %>
  </div>
  <%= f.submit class: "ui blue button inverted" %>
<% end %>